# These two are standalone scripts that read local radar/model files at import
# time; they are not unit tests and cannot run without that data.
collect_ignore = ["test_all_metrics.py", "test_all_metrics_with_plots.py"]
